export const generateFakeDriverAndCarProfile = (driverProfile, carProfile) => {

    const driverProfilekeys = Object.keys(driverProfile)
    driverProfilekeys.forEach((key => {
        const changeValueOfKey = Math.random() < .1
        if (changeValueOfKey) {
            // driverProfile.key = 
        }
    }))
    
    
    
}